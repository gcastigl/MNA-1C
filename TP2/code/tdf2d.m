function X = tdf2d(matrix, N, l, k)
	X = [];
   #N = length(matrix(1,:);
    for n = 1 : N
		for m = 1 : N
			c = e ** (i * 2 * pi / N );
    		X(n, m) = matrix(n, m);
	    endfor
    endfor
endfunction
